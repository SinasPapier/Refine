# Agentur-App

Interne Anwendung zur Verwaltung von Arbeitszeiten, Kunden, Zuständigkeiten und
fortlaufenden Nummernkreisen.

Privates Projekt – keine Nutzung durch Dritte vorgesehen.

## Entwicklung

```bash
npm install
npm run dev
```

Konfiguration über `.env` (siehe `.env.example`).
